# MVP build contract — 2026-09-12

Authorized: implement Rebobina MVP in this checkout; inspect/reuse authorized service credentials privately. Do not alter other projects. No automatic customer messages or payments. Public deployment requires secrets protected and session isolation.

Stack: React + TypeScript + Vite, Node Express, shared deterministic engine, Zod validated model output, PostgreSQL through server only when connection verified. Isometric SVG composed from constrained scene specification: generative facts/roles/layout, not runtime-generated executable code. Four modular page components. Open Design CLI absent; timed local endpoint check; use already agreed original warm world + restrained workbench direction if unavailable. Sites not selected: existing project, user requests reusable source and Vercel-compatible integration.

Acceptance: generate scene from context, verify quote provenance, choose actions, deterministic consequence, rewind without destroying immutable attempt, create/revise artifact, persist after reload, keyboard/mobile flows, no secret in bundle. Credential status is not authenticated capability until tested. Separate synthetic exercise assumptions from user facts. World unknown fields remain null; do not invent policy.

KPI: complete context → material → scenario → decision → replay → revision. Visual KPI: world visible in first viewport; interface operational not a landing page. Technical: build and deterministic tests pass; real API generation tested; errors/fallback labelled. Accessibility: equivalent text controls, labels, reduced motion.

Bounded build, no arbitrary professions engine, no assets marketplace, no voice dependency. Existing docs/research preserved. Each page can be refined separately after shared contracts stabilize.
