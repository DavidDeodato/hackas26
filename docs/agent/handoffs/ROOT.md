# ROOT — integração/base

STATUS: working. Dono: fork (8), continuação autorizada pelo usuário.

PUBLIC READY FUNCIONAL:17:13:26Z suite `REBOBINA_BASE_URL=https://rebobina-h4h.vercel.app npx tsx scripts/root-integration.ts` encerrou exit0 PASS_LIVE_INTEGRATION em10checks reais; resultado `artifacts/qa/root-integration-public.json`. Corrigido e comprovado erro ESM anterior. Coordenador informado. Não há bloqueio funcional público conhecido nesta suíte. Próximo: QA visual público e apresentação/vídeo conforme critérios, sem confundir fonte sintética com perfil real ou ensaio com previsão.

FIX PUBLIC API: log do coordenador confirmou ERR_MODULE_NOT_FOUND /var/task/server/index. Entry api corrigido pelo coordenador; ROOT padronizou imports relativos server/shared em .js. Build e16testes passaram; `scripts/root-serverless-check.ts` com VERCEL=1 importou app/PG sem listener e encerrou exit0. Redeploy solicitado ao coordenador. Falha pública anterior NÃO está considerada resolvida até novo smoke na URL.

PUBLIC SMOKE BLOQUEADO: primeira URL https://rebobina-h4h.vercel.app retornou HTTP500 FUNCTION_INVOCATION_FAILED em /api/health às17:09Z; nenhum check API público passou. Evidência `artifacts/qa/root-integration-public.json`. Coordenador4 consulta logs. Hipótese ainda não confirmada: imports relativos sem extensão em ESM/serverless ou inicialização DB. Não promover URL como funcional. Script agora suporta REBOBINA_BASE_URL e mantém relatório público separado do local. Assim que runtime for corrigido/republicado, repetir o mesmo smoke real, sem segredos no output.

ÚLTIMA VERIFICAÇÃO: modo cinematic padrão inspecionado no navegador1265x712, asset renderizado e legenda Cenário ilustrativo visível. Clique Explorar em3D renderizou diorama WebGL quente; Voltar ao cenário restaurou imagem. Mesma pergunta e audiência ativa preservadas durante alternância. Gate de alternância funcional cumprido. Imagem ilustrativa não representa geração3D por pessoa. Coordenador4 informado para coordenar captura final; aprovação estética não inferida dessa verificação. Rail contém scrolls internos para pergunta/rationale longos, observação enviada ao coordenador.

VISUAL V2 supersede qualquer gate anterior: referência `design/references/rebobina-cinematic-studio-v1.png` inspecionada por ROOT. Aplicados tokens canônicos MARCA v2, navegação164px, texto marfim/CTA pêssego, Manrope/DM Sans. Build SpatialStage passou1875módulos. Screenshot real em1265x712 mostrou diorama3D renderizado, painel30% e shell; clique Aproximar mudou câmera e botão para Visão geral. Ainda distante da referência: materiais verdes no engine e pergunta duplicada/grande no palco; donos coordenador4/SALA notificados. Relato SALA de erro de import transitório divergiu da aba limpa ROOT funcionando; encaminhado coordenador. Não liberar vídeo final como visual aprovado enquanto comparação com referência está aberta.

BASE LIBERADA: build passou após entrega de mesa.css;16/16testes passaram, incluindo novo teste determinístico que omite alegação documental sem citação válida e rotula sugestões. root-store-check repetido encerrou exit0: CAS, cotaPG e grounding real passaram. Snapshot backend liberado ao coordenador4 para deploy. Shell/favicons escuros integrados; screenshot real detectou painel branco/título escuro no CSS SALA, dono notificado diretamente. Gate visual continua aberto; não gravar final antes correção e nova revisão. Próximo dono: SALA fecha estilos/SpatialStage com coordenador; QA valida; VIDEO recaptura. ROOT mantém backend estável4173 e evita alterações concorrentes.

ATUALIZAÇÃO 13:58 SP: o estado visual anterior foi reaberto por pedido do usuário. Shell já adota tokens escuros MARCA e logo dark, preservando dimensões. SALA/MESA atualizam seus componentes; build momentaneamente aguarda mesa.css importado pelo worker. Backend estável em 4173, PID45676. Regressão live pós-CAS/cota passou às16:56:59Z (root-integration.json). prepareWork agora usa saída estruturada separando documento/proposta/pendência, valida citações literais e omite afirmações documentais sem referência válida. Teste focal CAS/cota/grounding em execução. Não capturar visual final ainda; aguardar SALA/MESA+QA. CORE import agora estático para empacotamento serverless, supersedendo nota histórica abaixo.

CONFIRMADO 13:51 SP: `artifacts/qa/root-integration.json` PASS_LIVE_INTEGRATION. PostgreSQL, material, perfil IA com citações literais, perguntas IA, feedback IA, bloqueio de resposta duplicada, replay preservando original, persistência, isolamento de sessão, rejeição de fonte alheia, material Work v1/v2 preservados: todos passaram em chamadas reais. Fontes usadas eram explicitamente fictícias. Não é validação humana/educacional.

Deploy delegado ao coordenador chat4: ROOT preparou `server/index.ts` com export default app, sem listen/Vite quando VERCEL, trust proxy de um salto apenas nesse ambiente. Store falha fechado no deploy sem PostgreSQL; não anuncia persistência via arquivo efêmero. Chat4 é dono de api/index.ts, vercel.json, .vercelignore e deploy/env. ROOT não toca nesses arquivos.

13:51 SP: SALA/AUDIENCIA/MESA e marca integradas no shell. Novo build completo passou. Home e /ensaio abrem RehearsalPage; /audiencia disponível; atendimento em /atendimento. Servidor reiniciado com rotas do ensaio; singular e plural conferidos HTTP200 e mesmo estado PostgreSQL. CORE service encontrado. Smoke REAL em execução via `scripts/root-integration.ts`; resultado em `artifacts/qa/root-integration.json`. QA e VIDEO podem abrir suas abas e testar/capturar as partes prontas; não precisam esperar aprovação estética do ROOT.

13:47 SP: contrato canônico `shared/rehearsal-types.ts`. O duplicado `rehearsal.types.ts` agora reexporta o contrato, eliminando divergência. Implementadas rotas em `server/rehearsal-routes.ts` com POST audience/generate/answer/rewind e GET state, todos sob sessão existente e PostgreSQL. Novo estado fica em `Workspace.rehearsal`, retrocompatível. Fontes precisam pertencer à sessão; citações conferidas contra texto; respostas na ordem; replay cria nova sessão preservando a original.

CORE: somente precisa entregar `server/rehearsal/service.ts` conforme protocolo; importado dinamicamente. Até entrega, mutações IA retornam erro explícito, não mock. SALA/AUDIENCIA: endpoints prontos no código; servidor será reiniciado pelo ROOT após build. Nenhum outro agente reinicia.

Confirmado antes deste patch: aplicação de atendimento compilou; dez testes de motor passaram; PostgreSQL conectado; OpenAI autenticada; cenário de floricultura gerado ao vivo em cerca de 5s. Novo core ainda pendente de integração e QA. Não declaro pivot pronto.

Próximo: build, restart controlado e smoke das rotas; integrar páginas dos forks quando entregues; manter atendimento em /atendimento. Somente ROOT altera shell/global/router/server/store.
